const User = require('./user');
const Message = require('./message');
const Project = require('./project');
const Article = require('./article');

module.exports = { User, Message, Project, Article };